# 08 · Self-Attention 手算：Q / K / V、缩放点积、12 个头是怎么切出来的

> **接 07**（`attentions` 是每层"谁看了谁"的记录表，shape `[1, 12, 22, 22]`）
> 本篇要回答：**这张表是怎么算出来的**，token 的向量又是如何按这张表被"重新混合"的。
>
> **参考**：B 站「动手写 BERT 系列」self-attention 单集 `BV14B4y1x79R`；本地 notebook `bilibili_vlogs/fine_tune/bert/tutorials/06_attn_01.ipynb`；源码见 `github.com/chunhuizhang/bilibili_vlogs`（`06_attn_01.ipynb`）。

---

## 〇、30 秒直觉：注意力 = "查资料"

一句话版本：**Self-Attention 就是让每个词，从整句话里按"相关度"取信息来更新自己**。

07 里说过"每个词问自己：我跟谁关系最密切？打分 → 按分数加权平均"。本篇把这三句话翻译成矩阵运算。翻译的钥匙是一个图书馆比喻：

| 角色 | 图书馆里的意思 | 一句话 |
|---|---|---|
| **Q**（Query，查询） | 你心里带着的问题 | "我此刻在**找什么**" |
| **K**（Key，键） | 每本书封面上的索书标签 | "我**能匹配什么**" |
| **V**（Value，值） | 书里的正文 | "我**实际有什么**" |

你拿着 Q 去对比每一个 K：匹配度高的书，它的 V（正文）就多取一点；匹配度低的少取一点。最后每个词都得到一份"按相关度加权混合"的新内容。

> 注意这个"自"（Self）：Q、K、V **全部来自同一个输入序列**——不是拿外部问题去查文档，是句子自己内部互相查。

---

## 一、总公式与形状全景

BERT 的自注意力核心就一个公式：

```
Attention(Q, K, V) = softmax( Q · Kᵀ / √d_k ) · V
                        └──── 权重 W ────┘
```

记 `hidden = 768`（d_model），BERT-base 用 **12 个头**，所以每个头：

```
d_k = 768 / 12 = 64
```

以第 2 步喂进去的 bank 句为例（batch=1、22 个 token，先暂时丢掉 batch 维看单样本，`N=22`），形状一路流动：

| 步骤 | 形状 | 是什么 |
|---|---|---|
| 输入 embedding `X`（07 的 `hidden_states[0]`） | `[22, 768]` | 22 个词，每词 768 维 |
| 乘三个矩阵得 `Q/K/V` | `[22, 768]` × 3 | 每个词的"问题/标签/正文" |
| 切成 12 个头，看其中 1 个头 | `[22, 64]` × 3 | 每个头只处理 64 维子空间 |
| 打分 `Q · Kᵀ` | `[22, 22]` | 第 i 行 = 第 i 个词看全句 22 个词的原始分 |
| `÷ √64` 再 `softmax` | `[22, 22]`，**每行和为 1** | 真正的注意力权重 |
| `权重 · V` | `[22, 64]` | 第 i 行 = 第 i 个词按权重混合后的新向量（单头） |
| 12 个头拼回去 | `[22, 768]` | 多头注意力输出（再过一层投影 W_o） |

> **shape 记忆锚**：`[N, 768]` 进来，`[N, 768]` 出去——**形状不变**，变的只是每个词的数值（07 说的"原地更新"）。中间那张 `[N, N]` 就是 07 里 `attentions` 的 `[1, 12, 22, 22]` 中的每个 `[22, 22]`。

---

## 二、跟着 notebook 一步步手算（真模型，不是玩具）

notebook 用的不是手写的小矩阵，而是 **`bert-base-uncased` 真实权重**（注意 07 用的是中文 `bert-base-chinese`；本篇 notebook 取英文模型，道理完全一样，`seq_len` 那封 newsgroup 邮件截断到 201）。

### 2.0 准备：取出第 0 层，盯住第 0 个头

```python
import torch
from transformers import BertModel, BertConfig

config = BertConfig.from_pretrained("bert-base-uncased")
model  = BertModel.from_pretrained("bert-base-uncased", config=config)
model.eval()

# 用 04 的同一套 tokenizer 拿 input_ids / token_type_ids / attention_mask
# 设输入为 201 个 token：X_emb 是 07 里 hidden_states[0] 的等价物
emb_output = model.embeddings(input_ids, token_type_ids)[0]   # [201, 768]

# 目标：第 0 层的第 0 个头
layer = model.encoder.layer[0]
```

### 2.1 先弄清楚一件事：为什么是 `@ Wᵀ`

`nn.Linear` 存权重的形状是 `(out, in)`，但前向是 `x @ Wᵀ + b`：

```python
Wq = layer.attention.self.query.weight     # [768, 768]  ← (out, in)
bq = layer.attention.self.query.bias       # [768]

# Linear 前向：emb [201,768] @ Wq.T [768,768] → [201,768]
```

### 2.2 切出"第 0 个头"的 Q、K、V

**头是怎么切出来的**：一个 `[768, 768]` 的 `Wq`，它的 **768 个输出维 = 12 个头 × 每头 64 维**。第 0 个头用的就是**前 64 列**：

```python
Wq_0 = Wq[:, :64]      # [768, 64]  第 0 个头的"查询投影"
bq_0 = bq[:64]         # [64]

Q_0  = emb_output @ Wq_0.T + bq_0      # [201, 64]
K_0  = emb_output @ Wk_0.T + bk_0      # [201, 64]  同理取前 64 列
V_0  = emb_output @ Wv_0.T + bv_0      # [201, 64]
```

> 数学上等价写法：`emb_output @ Wq.T[:, :64]`（先把整个 Wq 转置，再取前 64 列）。HuggingFace 真正实现用 `view/transpose` 一次切出 12 头；notebook 用**列切片**是把同样的数学讲给你看，两者结果一致。

### 2.3 打分：`Q · Kᵀ / √64`，再过 softmax

```python
d_k = 64
attn_weights = (Q_0 @ K_0.T) / math.sqrt(d_k)     # [201, 201]
attn_weights = torch.softmax(attn_weights, dim=-1)

# 验证：每一行（每个词）对全句的注意力加起来必须 = 1
attn_weights.sum(dim=-1)     # 全部 ≈ 1.0
```

`attn_weights[i, j]` 的含义：**第 i 个词（查询方）分配给第 j 个词（被看方）的信息占比**。行和为 1 = "每个词把 100% 的注意力配额分给全句"。

### 2.4 混合：`权重 · V`

```python
context_0 = attn_weights @ V_0      # [201, 64]
```

第 i 行 = `Σ_j weights[i, j] · V[j]`：把自己看得最重的那些词的"正文"，按权重掺到一起。

> **回到 bank 例句**（07 的三兄弟）："bank(河岸)"发 Query 出去找，"river / fishing / bank"的 Key 匹配度高 → softmax 后它们权重高 → 混合出的新向量里带着"水边"的气息；而"bank(劫匪)"那一路高权重给的是"robber / vault"。**同一个词，因上下文不同，查到的内容不同，更新后的向量自然不同**——这正是 07 验证"三个位置三个向量"的内部机制。

---

## 三、三个"为什么"（本集最该记住的）

### 3.1 为什么要有 Q、K、V 三个，而不是一个？

因为**打分和取内容必须分工**：

- 打分用的是 Q×K 的匹配度（"你的问题合不合我的标签"）；
- 取内容用的是 V（"把正文端出来"）。

如果只有一摊向量既当标签又当正文，那"查到的内容"就会和"匹配依据"互相绑定，模型没法学习"**我该看谁**"和"**看了之后拿什么**"两件独立的事。三个不同的可学习矩阵（`Wq / Wk / Wv`）让这三种角色各练各的。

### 3.2 为什么除 `√d_k`？

`Q`、`K` 里每个数大约在 ±1 附近，两个 64 维向量做点积，**64 项正负相加，方差随维数增长**——结果可能被放大到 ±8、±10（量级约 `√d_k`）。而 softmax 对很大的输入会"饱和"：最大值占近 100%、其余趋 0、梯度也趋 0，模型学不动。

除以 `√64 = 8` 把分数拉回 ±1 量级，softmax 曲线还处在"区分度好、梯度大"的区域。

> 直觉：不管头多宽（64/128…），**点积的平均大小总是随维数膨胀**，`÷√d_k` 就是给分数"统一降压"。这一下让 softmax 输入保持稳定，训练才顺。

### 3.3 为什么 12 个头？每头凭什么只拿 64 维？

- **算力账**：与其让一个头在 768 维上做一次全局注意力，不如切成 12 份、每份 64 维**并行**做 12 组注意力——总计算量一样，但能学 12 组**不同的匹配规则**。
- **分工账**：不同头倾向学会不同关系（有人盯指代、有人盯局部句法、有人盯语义相关），12 个头 = 12 种"从不同角度查资料"同时进行，最后拼起来。
- 因为每头只看 64 维，单头"只懂一个窄角度"反而是特性：逼它们分工，而不是每头都做同样的大而全匹配。

---

## 四、从单头回到完整的一层（08 → 03 架构闭环）

手算只覆盖了"核心 64 维单头"，要回到 07 里的完整输出，还得补三步：

1. **拼头**：12 个头各自的 `[N, 64]` 沿最后一维拼成 `[N, 768]`；
2. **输出投影**：再乘 `W_o`（`[768, 768]`）做一次混合（HuggingFace 里就是 `layer.attention.output.dense`）；
3. **残差 + LayerNorm + FFN**：`X + AttnOutput → LN → FFN(768→3072→768) → +残差 → LN`，这才是完整的一个 `BertLayer` 出口（对应 03 画的层结构）。

另外真实推理时还有 **padding mask**：句子 pad 到 22 的地方没有真词，score 会在 softmax 前被置成很大的负数（如 `-10000`），让它们的权重 ≈ 0，**不能参与"看与被看"**。

> 对照 07：`attentions[0]` 的形状 `[1, 12, 22, 22]` = 第 0 层 12 个头各一张"行和为 1"的表，就是这里的 `attn_weights`。

---

## 五、常见误区（手算完最容易踩的 4 个坑）

1. **误区：`Q · Kᵀ` 就是注意力权重。**
   不是。那是**原始分（score）**；必须过 `softmax`、每行和为 1 之后才是"权重"。07 里 `attentions` 存的是 softmax 之后的版本。
2. **误区：Q = K = V，因为它们都来自同一个输入。**
   不是。"都来自同一个输入"叫**自**注意力；但它们各自乘了**不同的可学习矩阵** `Wq / Wk / Wv`，学成三个不同角色（问题 / 标签 / 正文）。绝不是一个矩阵复制三份。
3. **误区：每个头都在完整的 768 维上做注意力。**
   不是。每头只有 **64 维**（`768 ÷ 12`），各自在低维子空间工作，拼回去才看全 768。这也是"多头"和"一个大头"的本质差别。
4. **误区：手算完 64 维核心就以为一层只有注意力。**
   核心之外还有：拼头、`W_o` 输出投影、残差连接、LayerNorm、FFN。08 手算的是"最关键的引擎"，不是整台车。

---

## 六、记忆卡

- 公式一句话：`权重 = softmax(Q·Kᵀ / √d_k)`，`新向量 = 权重 · V`
- 三角色：**Q = 我想找什么，K = 我能匹配什么，V = 我有什么**；score 高 = 我重点取你
- "自"在 Self-Attention 里 = Q、K、V 全来自**同一个输入序列**
- 行和为 1 = 每个词把 100% 的注意力配额分给全句（含自己）
- 手算最小闭环：`[N,768] → 单头 [N,64] → 分数 [N,N] → softmax → 混合 [N,64]`

---

## 七、自测（合上文档，先答再对）

1. 一句话向没学过的人解释：为什么"bank"在河岸句和劫匪句里最终是两个向量？
2. `attn_weights` 形状 `[22, 22]`，第 7 行第 10 列这个数代表什么？把这一行全加起来应该等于几？
3. 如果把 `d_k` 从 64 换到 128 而不除以 `√d_k`，softmax 前的分数会怎样？为什么？

<details>
<summary>参考答案</summary>

1. 每个词会向全句发"查询"并按相关度取信息；bank(河岸) 的重点取到了 river/fishing 的内容，bank(劫匪) 取到的是 robber/vault，混合后的向量自然分道扬镳。
2. 第 7 个词（查询方）分配给第 10 个词（被看方）的信息占比；行和 = 1（softmax 归一化过）。
3. 分数整体变大（量级约 `√128`），softmax 输入进入饱和区、梯度趋近 0，模型很难学。所以要么除 `√d_k`，要么换别的缩放手段。
</details>

---

## 八、费曼三连问（合上文档自测）

1. **一句话**：Self-Attention = 每个词先拿"问题 Q"去比全句的"标签 K"得到一张"我该看谁"的权重表，再按这张表去取各词的"正文 V"，混合成自己的新向量。
2. **手推**：任取两个 token 的向量 `x₁、x₂`，写出这 2 个词的单头 score 矩阵（`[2,2]`）每一格的代数式，并指出 softmax 在哪一维做、为什么。
3. **讲出去**：给一个只会"余弦相似度"的人解释，为什么 Q、K、V 要三个矩阵而不是直接拿原向量相似度做权重——重点说清"匹配依据"与"取出内容"为什么要分离。

---

## 九、与前面笔记的连接

- **接 07**：07 看到 `attentions` 这张"谁看了谁"的表，08 算出它怎么来的；07 的 13 工位里"每层 22 个词互看一遍"，看的动作就是本篇的"打分 + 混合"。
- **接 05**：`hidden_states` 每个工位进出的 `[1, 22, 768]` 形状不变，就是本篇"形状不变、数值重混"的宏观视角。
- **接 03（架构）**：本篇第 4 节补全了"一个 BertLayer = Self-Attention + 残差/LN + FFN"的完整闭环。
- **下一步 09 预告**：自注意力只解决"谁跟谁相关"，还没编码"位置"——下一个问题就是 Positional Embedding 与绝对/相对位置编码。

---

*08 完。动手跑的路径：`notebook` 里 `06_attn_01.ipynb`，看完本篇再去跑一遍，重点盯 `attn_weights.sum(dim=-1)` 那一格输出。*
